{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Scoped capabilities for the TinyDrop main window. Filesystem access is intentionally limited to user-selected paths via the dialog plugin; we do NOT request broad filesystem capabilities. See ARCHITECTURE.md.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:webview:default",
    "core:event:default",
    "core:app:default",
    "core:resources:default",
    "core:menu:default",
    "core:tray:default",
    "core:path:default",

    "dialog:default",
    "dialog:allow-open",

    "opener:default",
    "opener:allow-reveal-item-in-dir",
    "opener:allow-open-path"
  ]
}